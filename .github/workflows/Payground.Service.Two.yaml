name: Research

on: 
  workflow_dispatch:
  push:
    branches: 
      - main

jobs:
    build:
      env:
        TestEnvOne: "JobLevelEnvVar"
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v3

        - name: Environment Variables
          run: printenv

        - name: Print Env Var
          run: |
            echo $GITHUB_ACTOR
            echo $TEST_ENV_ONE
           
     


    
    